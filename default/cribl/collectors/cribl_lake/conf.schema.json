{
  "type": "object",
  "title": "",
  "required": ["dataset"],
  "properties": {
    "type": {
      "type": "string",
      "enum": ["cribl_lake"],
      "flag": {
        "prefix": "destination",
        "modes": ["single", "worker"]
      }
    },
    "dataset": {
      "type": "string",
      "title": "Lake dataset",
      "description": "Lake dataset to collect data from."
    }
  }
}
