{
  "rules": {
    "ui:field": "Table",
    "ui:options": {
      "popupEditor": "RegexEditor",
      "popupEditorTitle": "Event Breaker Rule",
      "matchRegexProperty": "matchRegex",
      "replaceExprProperty": "replaceExpr",
      "eventFieldProperty": "fields"
    },
    "items": {
      "matchRegex": {
        "ui:widget": "RegexInputWithoutPopup",
        "ui:placeholder": "Enter regex",
        "ui:options": {
          "columnWidth": "42%"
        }
      },
      "replaceExpr": {
        "ui:widget": "JsInputWithoutPopup",
        "ui:placeholder": "Enter replace expression",
        "ui:options": {
          "columnWidth": "42%"
        }
      },
      "disabled": {
        "ui:title": "Enabled",
        "ui:options": {
          "columnWidth": "15%",
          "inverted": true
        }
      }
    }
  },
  "fields": {
    "ui:field": "Tags",
    "ui:placeholder": "Field names"
  },
  "depth": {
    "ui:depth": "Depth",
    "ui:placeholder": "Enter depth"
  },
  "flags": {
    "ui:field": "Table",
    "items": {
      "name": {
        "ui:placeholder": "Enter field name"
      },
      "value": {
        "ui:widget": "JsInputWithoutPopup",
        "ui:placeholder": "Enter value expression"
      }
    }
  },
  "ui:options": {
    "groups": {
      "advanced": {
        "title": "Advanced Settings",
        "collapsed": true,
        "properties": ["flags", "depth"]
      }
    }
  }
}
